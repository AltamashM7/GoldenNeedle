# Decision log

These decisions describe the current product and architecture direction. A later Orchestrator instruction may explicitly change an **OPEN / MAY CHANGE** item.

Current status authority: `Docs/current-state.md`.

Detailed pre-Phase-5A foundation architecture: `Docs/pre-phase5a-foundations.md`.

| Decision | Status | Rationale |
|---|---|---|
| Unity 6.5 | **LOCKED** | Establishes the engine baseline for SIH 2026 development and avoids version drift. |
| URP | **LOCKED** | Provides the current Unity rendering baseline while supporting a deliberately simple, CPU-conscious presentation target. |
| CPU-first / no required dedicated GPU | **LOCKED** | The application must run acceptably on an ordinary computer without requiring a discrete GPU. |
| Camera experience integrated into Unity | **LOCKED** | The user-facing product must provide one cohesive application rather than require an external tracking program. |
| Pose provider remains replaceable | **LOCKED ARCHITECTURAL DIRECTION** | The provider boundary protects the rest of the project from backend changes. Stock MediaPipe/TFLite and OpenVINO CPU FP32 are both available behind the current provider integration. |
| Full-body digital embodiment, not merely gesture-triggered buttons | **LOCKED PRODUCT DIRECTION** | Continuous body-driven avatar control is the core innovation and fitness interaction model. |
| Engine-owned canonical skeleton | **LOCKED ARCHITECTURAL DIRECTION** | Downstream systems need a stable project-owned body representation independent of provider-specific results. |
| Pose and locomotion are separate systems | **LOCKED ARCHITECTURAL DIRECTION** | Body reproduction and conversion of actions such as jogging in place into world movement have different semantics and tuning needs. |
| Modular Hub/course structure | **LOCKED ARCHITECTURAL DIRECTION** | Independent environments and courses reduce coupling and support parallel content ownership. |
| Git LFS enabled from initial repository setup | **CURRENT REPOSITORY DECISION** | The local Git LFS client and repository filters/endpoint are present; current `.gitattributes` has no LFS-managed patterns, so this state is preserved and reported rather than expanded speculatively. |
| GitHub Desktop for user's normal Git operations | **CURRENT WORKFLOW DECISION** | The USER uses GitHub Desktop for normal branch, commit, push, and PR-related mutations. |
| Web Orchestrator + Web Builder / Luna as scoped builders | **CURRENT DEVELOPMENT WORKFLOW** | Architecture/integration review is kept separate from implementation. Complex fixes belong to the intelligent Web Builder; Luna is for tightly scoped routine work. |
| Official Unity Codex plugin `unity@unity-agent-plugin` | **CURRENT DEVELOPMENT TOOLING DECISION** | Provides Unity-specific Codex skills through the first-party Unity Technologies integration; it remains outside the Golden Needle runtime. |
| Official Unity CLI + Pipeline package `com.unity.pipeline` `0.5.0-exp.1` + Unity MCP | **CURRENT DEVELOPMENT TOOLING DECISION** | Gives Codex a supported path to inspect and perform harmless operations in the live Unity Editor while keeping the integration out of shipped product dependencies. |
| Integrated laptop webcam is a supported baseline input | **LOCKED SPIKE REQUIREMENT** | The technical baseline must work with an ordinary built-in webcam; external or depth hardware is optional. |
| Partial-body tracking is valid | **LOCKED SPIKE REQUIREMENT** | Upper-body control data remains usable when legs are outside the frame; trust is evaluated per landmark and missing lower-body landmarks do not reject the whole pose. |
| MediaPipeUnityPlugin `0.16.3` + MediaPipe `0.10.22` semantics remain the compatibility baseline | **LOCKED CURRENT COMPATIBILITY RULE** | Current preprocessing, ROI/tracking, landmark decoding, world-landmark semantics and canonical behavior are the known-good reference and are reused even when neural inference is accelerated. |
| Stock MediaPipe/TFLite CPU backend remains available | **LOCKED FALLBACK RULE** | The known-safe TFLite path must not be deleted or weakened merely because OpenVINO is faster on the current proof machine. |
| OpenVINO CPU FP32 backend | **USER-VALIDATED BEST-TESTED LOW-END BACKEND** | Additive OpenVINO 2026.3.0 integration reuses MediaPipe graph semantics and materially improves live compute performance on the i3-7100U / HD620 test machine. It does not silently replace the fallback or serialized/default policy. |
| OpenVINO exact detector/landmark models | **LOCKED INTEGRATION RULE** | The runtime uses the exact models extracted from the production task bundle; no detector densification/conversion or alternate weights are allowed without a separate equivalence decision. |
| OpenVINO persistent worker + two-slot latest-frame mailbox | **USER ACCEPTED — PASS** | One active inference plus one replaceable newest pending frame removes avoidable Update-bound launch delay while preserving no-backlog/latest-frame semantics. No FIFO/history/replay/catch-up queue is allowed. |
| WebCamCPU/GetPixels32 acquisition for OpenVINO | **USER ACCEPTED — PASS FOR CURRENT MILESTONE** | Reused CPU camera pixels + reusable 320x240 preparation removes the dominant AsyncGPUReadback latency on the USER machine and enables roughly 27–29 fresh poses/s with a ~30 FPS camera. |
| ExistingReadback acquisition remains available | **LOCKED FALLBACK / REFERENCE RULE** | The older readback path remains selectable for fallback, comparison, and hardware-specific compatibility. |
| Body inference target 320x240 | **CURRENT BEST-TESTED BASELINE** | It preserves tracking quality while reducing upstream preparation cost; lowering the image further does not shrink the fixed 224/256 neural tensor sizes. |
| Bounded latest-frame camera/inference scheduling | **LOCKED RUNTIME RULE** | At most one active inference and one replaceable newest prepared/pending frame are allowed. Latest useful frame wins; no historical frame queue or inference backlog is permitted. |
| Motion Engine long-lived phase branch | **CURRENT WORKFLOW DECISION** | `engine/pose-tracking-spike` carries core Motion Engine work until the USER explicitly approves a later merge strategy; checkpoints are not mechanically merged to `main`. |
| Phase 2 canonical joint set and partial validity | **CURRENT PHASE 2 IMPLEMENTATION / PRESERVE AS V1** | Engine-owned frame contains exactly the accepted 20 named canonical joints plus trusted midpoint derivations; missing joints do not invalidate the whole frame. Foundation C preserves `CanonicalPoseFrame.JointCount == 20` and all existing numeric meanings. |
| Phase 2 canonical coordinates | **CURRENT PHASE 2 IMPLEMENTATION** | Image x increases right and y increases up. Canonical 3D uses +X camera/view right, +Y up, +Z away. Conversion occurs once at the provider-to-canonical boundary. |
| Single canonical camera / inference frame | **PHASE 4 USER ACCEPTED** | Front-facing status does not request horizontal inference mirroring; vertical/rotation transport remains. Display mirror is presentation-only and canonical normalized/world mapping is unchanged. |
| Phase 4 signed canonical-to-avatar mapping | **PHASE 4 USER ACCEPTED** | Corrected frontal source semantics are `R≈-X, U≈+Y, F≈-Z`; the accepted signed reference mapping drives procedural and Animator Humanoid paths. |
| Phase 4 positional limb retargeting + analytic IK | **PHASE 4 USER ACCEPTED / PRODUCTION POSITIONAL AUTHORITY / FALLBACK REFERENCE** | Normalized limb targets map through the accepted reference basis and preserve avatar-authored proportions; project-owned analytic two-bone IK remains production-authoritative for endpoint positions. Foundation E adds only endpoint-preserving post-solve orientation/detail refinement. |
| Phase 4 modular measurement calibration | **PHASE 4 USER ACCEPTED** | Comfortable body reference establishes torso axes/proportions and each limb chain accumulates independent measurements; missing limbs do not globally block motion. |
| Stable Phase 3 canonical filter remains calibration/locomotion authority | **LOCKED CURRENT BOUNDARY** | Calibration and Phase 5A continue to consume the accepted `1.0 / 0.05 / 1.0` stabilized frame even while avatar-only responsiveness is tuned independently. |
| Avatar-drive pose source is independently selectable | **IMPLEMENTED / PRESERVE FOR TUNING** | Avatar solving can use Stable, Raw, or responsive One Euro variants without changing calibration or locomotion. The selected frame is shared consistently by rotation solving, kinematic targets and torso/source mapping. |
| Avatar-drive serialized enum meanings | **LOCKED COMPATIBILITY RULE** | `Stable=0`, `Raw=1`, `A=2`, `B=3`, `C=4`, `D=5` are preserved so existing scene/settings serialization does not silently change meaning. |
| Avatar responsive profiles A/B/C/D | **IMPLEMENTED / FINAL WINNER DEFERRED** | A/B proved the latency-stability tradeoff; C/D isolate beta with `minCutoff=1.0`. The USER intentionally deferred final fine-tuning and default selection. |
| Raw avatar-drive mode | **PRESERVED LATENCY REFERENCE** | USER reports Raw feels effectively instant but slightly less stable. It remains a valuable upper-bound responsiveness reference rather than being forced as the production default. |
| Humanoid render-rate presentation smoothing | **IMPLEMENTED / SEPARATE TUNING LAYER** | Exact Phase 4 solve remains authoritative. Foundation E post-solve detail is designed to be applied before the final solved target is captured so existing presentation smoothing converges toward the combined solve rather than fighting it. Tracking, calibration, IK targets and locomotion remain unaffected. |
| Motion-engine latency/performance optimization milestone | **CURRENT MILESTONE COMPLETE; FURTHER TUNING DEFERRED** | Major bottlenecks were removed and the best tested path approaches camera cadence. Additional smoothing/performance tuning is optional future work, not a blocker to normal development. |
| Phase 5A hybrid locomotion | **IMPLEMENTED / NOT USER ACCEPTED** | Finite support-foot camera-space displacement and cadence extension are fused while Phase 4 pose reproduction stays separate. Acceptance work resumes only after the approved pre-Phase-5A foundations are handled. |
| Phase 5A root estimation | **IMPLEMENTED / NOT USER ACCEPTED** | Physical translation authority is the ankle/heel/toe support-base model. Torso apparent scale is auxiliary depth corroboration only. |
| Phase 5A cadence fusion | **IMPLEMENTED / NOT USER ACCEPTED** | Cadence extends range only when rhythmic lower-body stepping exists and meaningful physical root velocity is small; physical motion suppresses cadence to avoid obvious double-counting. |
| Phase 5A recenter | **IMPLEMENTED / NOT USER ACCEPTED** | Public `Recenter()` makes the current physical location the new tracking origin while preserving current virtual X/Z. |
| Phase 5A vertical/root authority | **LOCKED PROTOTYPE RULE** | Locomotion writes avatar-root X/Z only; root Y and rotation remain excluded. |
| Phase 5A physical coordinate mapping | **IMPLEMENTED / NOT USER ACCEPTED** | Camera-space physical displacement is scaled then mapped through the accepted Phase 4 reference canonical-to-avatar axis map before world X/Z application. Live body heading affects cadence only. |
| Phase 5A physical scale defaults | **FIRST-QA TUNING DECISION** | Current lateral/depth defaults are `0.9 / 1.5`; they remain Inspector-tunable rather than production-locked. |
| Phase 5A support model v3 | **IMPLEMENTED / NOT USER ACCEPTED** | Common `(L+R)/2` support displacement drives room movement while differential `(L-R)/2` gait evidence attenuates depth trust. |
| Phase 5A known lean-translation issue | **OPEN / MUST RESOLVE BEFORE ACCEPTANCE** | USER QA found that planted-feet leaning can still produce unwanted translation. |
| Phase 5A stationary cadence robustness | **OPEN / MUST RESOLVE BEFORE ACCEPTANCE** | USER QA found cadence stepping while stationary is not yet robust enough. |
| Phase 5A F12 Lab/Game mode | **IMPLEMENTED / NOT USER ACCEPTED** | F12 is presentation-only. Lab keeps webcam/debug IMGUI; Game hides IMGUI and uses the persistent third-person camera. Foundation B preset selection remains orthogonal to this mode toggle. |
| Tracking vs presentation data | **LOCKED BOUNDARY** | Phase 5A consumes genuine stabilized tracking data; smoothed avatar rotations are presentation-only. |
| External/phone camera selection | **IMPLEMENTED** | Persist WebCamDevice name; Inspector enumerates devices and `V` cycles ordinary devices. Phones require OS/UVC/virtual-webcam exposure; no phone-specific network protocol exists. |
| Manual camera rotation | **IMPLEMENTED** | Auto follows `videoRotationAngle`; optional 0/90/180/270 overrides drive inference preparation and Lab display/convention state. Front-facing metadata still does not imply horizontal inference mirror. |
| Camera hot-switch safety | **IMPLEMENTED** | Pending switch blocks new old-camera work, waits for active work, restarts one provider pipeline, bumps source/session convention version, and invalidates calibration/Phase 5A origin assumptions. |
| Pose target cadence 30 FPS | **CURRENT PROTOTYPE TARGET** | 30 FPS is a maximum/request target, not a result-rate promise. With OpenVINO + WebCamCPU, full-body USER evidence reaches roughly 27–29 fresh results/s on the current proof machine. |
| Pipeline timing diagnostics | **IMPLEMENTED / RETAIN** | F7 exposes capture/render/request/result data, acquisition and inference timing, pose age, launch-origin/scheduling evidence and fallback information without per-frame log spam. |
| Lab clear-only camera | **IMPLEMENTED / RETAIN** | In Lab, the persistent Camera remains enabled with culling mask zero and solid-color clear so Unity does not show `No cameras rendering`, while IMGUI retains fitted whole-frame webcam geometry. F12 restores the original world render settings. |
| Sentis GPUCompute on Intel HD 620 | **REJECTED FOR CURRENT LOW-END BACKEND** | Same-run evidence showed Sentis GPUCompute slower than CPU and D3D12 emitted fence warnings. This does not prohibit stronger GPUs from being evaluated later. |
| Custom native Windows camera capture | **DEFERRED** | WebCamCPU/GetPixels32 already removed the dominant readback bottleneck without adding a platform-specific camera stack. |
| Pre-Phase-5A foundation track | **A/B/C/D ORCHESTRATOR CODE-AUDITED; E RUNTIME-WIRING CORRECTION REQUIRED; USER MANUAL QA DEFERRED** | Unified commands/speech, camera presets, rich canonical orientation and MediaPipe hand landmarks have cleared independent code audit. Foundation E's core/detail math is implemented and verified, but the live spike does not currently instantiate `RichHumanoidDetailRetargeter`; fix that narrow runtime composition blocker before comprehensive A–E USER QA. |
| Unified command/action layer | **IMPLEMENTED / ORCHESTRATOR CODE-AUDITED / USER MANUAL QA DEFERRED** | Keyboard, speech, UI and future inputs invoke shared runtime actions rather than duplicating behavior or simulating key presses. |
| Speech commands | **IMPLEMENTED / ORCHESTRATOR CODE-AUDITED / USER MANUAL QA DEFERRED** | Speech remains a replaceable input backend with Inspector-editable phrase→action mappings, cooldown, optional wake prefix, confidence thresholds and graceful unsupported-backend behavior. Real microphone QA is deferred. |
| Foundation A verification workflow | **READ-ONLY / LOCKED HYGIENE RULE** | The permanent Foundation A workflow uses `contents: read`, performs smoke/static regression checks only, and must never auto-apply migrations, commit, or push repository changes. |
| Camera view presets | **IMPLEMENTED / ORCHESTRATOR CODE-AUDITED / USER MANUAL QA DEFERRED** | One primary `ThirdPersonLabCamera` owns data-driven Back/Front/Left/Right/FullBody/Hands/LeftHand/RightHand presets, retained-heading placement, smooth transitions and safe semantic focus fallbacks. Speech invokes selection through the shared command layer; preset selection never toggles Lab/Game. The Orchestrator corrective audit also fixed and permanently guards the `CanonicalBoneId` Rotation namespace dependency. |
| Camera preset compatibility baseline | **LOCKED FOUNDATION B RULE** | `Back` is seeded from the previous third-person values (distance 4.0, height 2.2, look height 1.15, response 7.0, FOV 55) so Foundation B does not silently replace the established F12 Game View behavior. |
| Camera preset focus fallbacks | **LOCKED FOUNDATION B RULE** | Hands prefers the midpoint of two valid hands, then one available hand, then body/root. Left/Right hand prefer their chain tip, then lower arm, then body/root. Missing optional targets never invalidate tracking or rig binding. |
| Rich canonical motion representation | **FOUNDATION C IMPLEMENTED / ORCHESTRATOR CODE-AUDITED** | `RichMotionSchema` v1 adds fixed/preallocated provider-independent semantic evidence and 12 anatomical orientation channels while preserving CanonicalBodyV1 exactly. Source/schema/timing/confidence and observability metadata are explicit. USER manual/runtime QA remains deferred. |
| Canonical orientation authority | **FOUNDATION C IMPLEMENTED / LOCKED ARCHITECTURAL DIRECTION** | `CanonicalAnatomicalBasis` stores explicit primary/secondary/third axes, determinant, handedness and validity. Rich canonical source-of-truth contains no Quaternion; avatar quaternions are created only at the final retarget/application layer. |
| Optional rich source boundary | **FOUNDATION C IMPLEMENTED** | `IRichMotionEvidenceSource` is additive; `ICanonicalPoseSource` remains unchanged. Legacy-only sources therefore remain valid and rich-capable providers can expose extra evidence without making it mandatory engine-wide. |
| MediaPipe rich pose evidence reuse | **FOUNDATION C IMPLEMENTED / NO SECOND INFERENCE** | `MediaPipeCanonicalPoseSource` reuses its one persistent latest 33-landmark `PoseObservation` for both V1 and rich mapping. Numeric MediaPipe indices remain inside provider/mapping code; the rich core consumes semantic IDs only. |
| Foundation C semantic evidence set | **FOUNDATION C IMPLEMENTED** | Current pose evidence uses pelvis/chest midpoints plus shoulders, elbows, wrists, hips, knees, ankles, heels, toes, thumbs and pinkies. This is selected orientation evidence from the body pose stream, separate from Foundation D full hand tracking/finger articulation. |
| General bone-orientation reconstruction | **FOUNDATION C IMPLEMENTED / LOCKED ARCHITECTURAL DIRECTION** | One descriptor-driven solver covers pelvis/chest, bilateral upper/lower arms, upper/lower legs and feet. It uses primary direction plus independent secondary evidence, projection/orthogonalization, determinant/handedness validation and degeneracy checks rather than a forearm-only twist patch. |
| Swing/twist uncertainty handling | **FOUNDATION C IMPLEMENTED / LOCKED BEHAVIOR** | Twist states are `Observed`, `Held`, `ReferenceFallback`, and `Unobservable`. Trustworthy primary/swing continues when secondary evidence is absent or degenerate. Short loss holds/reprojects the last trusted secondary with reduced confidence; after the configured longer interval it moves toward a separately stored trusted reference around the current primary without claiming observation. |
| Foundation C rich reference state | **FOUNDATION C IMPLEMENTED / ADDITIVE CALIBRATION BOUNDARY** | Rich orientation keeps its own trusted reference/temporal state rather than mutating `MotionCalibrationProfile`. It resets on calibration begin/reset, coordinate-convention changes, and rich source/session discontinuities; optional rich orientation does not affect existing calibration validity. |
| Foundation C production application boundary | **FOUNDATION E CONSUMER BOUNDARY DESIGNED / RUNTIME WIRING PENDING** | `MotionEngineRuntime` computes rich motion only after accepted legacy rotation/kinematic outputs. Foundation E is designed to consume that data only as optional post-solve detail; it does not replace Phase 4 endpoint/IK authority or make rich data mandatory. The current spike still needs to instantiate the E consumer component. |
| Foundation C verification workflow | **READ-ONLY / ORCHESTRATOR AUDITED / CORRECTIVE CI GREEN** | `.github/workflows/foundation-c-rich-motion.yml` remains permanent read-only verification and retains provider isolation, optional-source, single-observation, stabilizer→calibration, legacy Phase 4 production-authority and no-canonical-Quaternion guards. Corrective run `34919859131` (job `104225315113`) passed at `9e4c4ac3a9d87eade06a43e2833481cce22b70f9`. Actual Unity Test Runner execution remains deferred. |
| MediaPipe hand landmarks | **FOUNDATION D IMPLEMENTED / ORCHESTRATOR CODE-AUDITED / USER MANUAL QA DEFERRED** | A separate MediaPipe Hand Landmarker is used rather than replacing the optimized body provider with Holistic. Perfect finger mocap is not required; the milestone provides useful palm/finger evidence while preserving body authority. |
| Canonical hand contract | **FOUNDATION D IMPLEMENTED / LOCKED ADDITIVE BOUNDARY** | `CanonicalHandFrame` owns exactly 21 semantic landmarks per hand, image-space evidence, hand-local metric 3D, side/association/confidence/timing, palm basis and derived articulation. Provider-native Hand Landmarker types/indices do not cross into hand core. |
| Hand palm/orientation authority | **FOUNDATION D IMPLEMENTED / REUSES CANONICAL BASIS RULE** | Palm orientation uses the same explicit `CanonicalAnatomicalBasis` authority introduced by Foundation C. No hidden canonical Quaternion source of truth is introduced. Degenerate palm evidence becomes unavailable rather than fabricating orientation. |
| Hand articulation summaries | **FOUNDATION D IMPLEMENTED / CONSERVATIVE SEMANTICS** | Finger curl features support useful `Open`, `Fist`, `Pointing`, `Intermediate`, and `Unknown` summaries. Classification is intentionally conservative and does not claim perfect finger mocap. |
| Hand side association | **FOUNDATION D IMPLEMENTED / POSE-WRIST FIRST** | Hand association prefers proximity to trustworthy accepted body pose wrists, supports one-wrist visibility, falls back conservatively to MediaPipe handedness, resolves duplicate sides conservatively, and can remain ambiguous instead of forcing unsafe identity. |
| Hand freshness and partial validity | **FOUNDATION D IMPLEMENTED / LOCKED SAFETY RULE** | Left/right hands are independently valid. Current defaults are `350 ms` maximum hand age and `200 ms` maximum body↔hand source skew. A stale/missing hand is unavailable for live fusion without invalidating the other hand or body stream. Coordinate/session changes clear held hand state. |
| Shared body/hand fusion timeline | **FOUNDATION D ORCHESTRATOR CODE-AUDITED / LOCKED SYNCHRONIZATION RULE** | The existing `MediaPipePoseProvider` `Stopwatch _clock` is the single semantic timing epoch for body and hand fusion. Hand Landmarker source timestamps, callback receive timing, canonical evaluation, hand age and body↔hand skew all use that provider timeline. `Time.unscaledTimeAsDouble` may drive the independent 12 Hz capture cadence only. The read-only timeline surface must not alter body/OpenVINO cadence, mailbox, acquisition, existing timestamps, serialized defaults or lifecycle. |
| Hand provider path | **FOUNDATION D IMPLEMENTED / SEPARATE CPU LIVE_STREAM** | `MediaPipeHandLandmarkerSource` uses MediaPipeUnityPlugin `0.16.3`, CPU `LIVE_STREAM`, `numHands=2`, default `12 Hz`, and separate `480x360` preparation. It samples the existing `WebCamTexture` with its own reusable buffers and does not enter or alter the body OpenVINO mailbox. |
| Hand latest-only scheduling | **FOUNDATION D IMPLEMENTED / LOCKED PERFORMANCE RULE** | Exactly one active hand inference plus at most one replaceable latest pending hand snapshot is allowed. No FIFO/history/replay/catch-up queue is permitted. Body tracking remains the priority stream. |
| Hand model provenance | **FOUNDATION D IMPLEMENTED / LOCKED BUNDLED ARTIFACT IDENTITY** | Official Google MediaPipe Hand Landmarker `float16/1`, exactly `7,819,105` bytes, SHA-256 `fbc2a30080c3c557093b5ddfc334698132eb341044ccee322ccf8bcf3607cde1`, is physically bundled at `Assets/StreamingAssets/GoldenNeedle/PoseTrackingSpike/Models/hand_landmarker.task`. Runtime prefers this verified bundle; the SHA-verified private-cache/official-download route remains optional fallback only. Model failure fails hands locally without failing body tracking. |
| Foundation D production application boundary | **LOCKED DATA-PRODUCER BOUNDARY** | Foundation D itself exposes hand data only and never owns avatar driving. Foundation E consumes the existing project-owned hand contract additively without changing D provider cadence/scheduling/model or Phase 4 body authority. |
| Foundation D verification workflow | **READ-ONLY / ORCHESTRATOR CODE-AUDITED / PERMANENT E-COMPATIBLE BOUNDARY GREEN** | The historical `FOUNDATION_E_NOT_STARTED` assertion has been replaced by `FOUNDATION_D_PRODUCTION_APPLICATION_BOUNDARY_PRESERVED`. The workflow keeps model identity, shared-timeline/monotonic timestamp, provider isolation, 21-landmark/hand-local/scheduler, body/OpenVINO/CanonicalBodyV1/scene and dirty-tree guards. Final compatibility run `34933354189`, job `104265988337`, succeeded at `087068dd14cd4bae11243a5efbd1bd5ee4cd309e`; the earlier accepted D audit run remains `34920144300` / `104226227007` at `864b39a520c78db1a0572b87a7d42c9da5cedaa4`. |
| Body tracking remains priority stream | **LOCKED PERFORMANCE RULE** | Hand inference is independently throttled/lower cadence and Foundation E is retarget math only; neither may casually destroy the near-camera-cadence body path or introduce unbounded queues. |
| Optional-bone/capability retargeting | **FOUNDATION E CORE IMPLEMENTED / RUNTIME-WIRING CORRECTION REQUIRED** | `HumanoidRigBinding` keeps legacy `IsBound`/`BoundBoneCount` body-only semantics and exposes a separate optional detail capability set for all 30 Animator Humanoid finger transforms. Zero, partial and full optional rigs remain valid; the legacy explicit body-source interface was not destructively expanded. Chain-tip bind local rotation is cached alongside existing tip position/scale so optional palm detail has a stable Phase-4/reference return baseline without affecting rig validity. |
| Foundation E post-solve integration | **CORE IMPLEMENTED / GENERIC BOUNDARY LOCKED / LIVE INSTANTIATION MISSING** | `IHumanoidPostSolveDetailLayer` remains a generic provider-independent boundary. `HumanoidRetargeter` executes existing discovered layers after the exact Phase 4 solve and before presentation smoothing, but it only scans already-existing components; the live `PoseTrackingSpike` currently does not instantiate `RichHumanoidDetailRetargeter`. |
| Foundation E rich limb refinement | **IMPLEMENTED / CODE-MATH VERIFIED / ENDPOINT-PRESERVING RULE** | Production E channels remain exactly bilateral upper/lower arms and upper/lower legs. Pelvis/chest/feet remain intentionally deferred. E adds only signed axial twist around the already solved segment axis; direct-child world orientation is compensated so Phase-4 endpoint geometry remains authoritative. Deterministic maximum endpoint residual remains `0` under a `<1e-6` threshold. |
| Foundation E rich reference/mapping | **IMPLEMENTED / CODE-MATH VERIFIED** | First trustworthy rich acquisition establishes a zero-delta reference. Relative axial twist is measured from `CanonicalAnatomicalBasis`, then signed through `CanonicalToAvatarAxisMap.DeterminantSign`, so reflected maps invert the axial sign. `Observed` updates, `Held` preserves trusted target, and `ReferenceFallback`/`Unobservable` neutralize only E's additive contribution without fabricating observation. |
| Foundation E palm target reference | **CORRECTED / CODE-MATH VERIFIED / LOCKED ABSOLUTE REFERENCE RULE** | Palm application is absolute rather than cumulative. Each hand keeps target reference axes derived from actual avatar index/middle/little proximal geometry and stored in the moving hand-parent's local frame; the current hand rotation delta is removed when the reference is captured. Each frame reconstructs the source-current vs source-reference orientation against that fixed target reference, combines it with the cached chain-tip bind local rotation, and smooths toward one absolute hand-local target. Repeating an unchanged source frame therefore converges rather than compounds, while lower-arm/parent motion carries the target reference in world space without changing the relative hand contribution. |
| Foundation E palm fallback/reset | **CORRECTED / CODE-MATH VERIFIED / LOCKED BASELINE RULE** | Stale/unavailable hands and palm-category disable invalidate that side's source palm reference and smoothly return palm detail toward the chain-tip Phase-4/reference local rotation. Foundation E master disable, post-solve reset, DriveRig disable and tracking/calibration loss remove palm detail through the reset boundary and restore the hand reference rotation. First trustworthy reacquisition is zero-delta before later relative motion resumes; left/right state remains independent. |
| Foundation E hand/finger application | **IMPLEMENTED / CODE-MATH VERIFIED / OPTIONAL CAPABILITY RULE** | Palm/finger application consumes `HandMotionRuntime` / `CanonicalHandFrame` only. Finger articulation remains geometry-based after the corrected palm target: Thumb maps CMC→MCP→IP→Tip; other fingers map MCP→PIP→DIP→Tip; only available target segments are driven by shortest swing with no manufactured finger axial twist. Stale fingers return independently toward reference. |
| Foundation E runtime/performance boundary | **LOCKED** | Foundation E owns no inference, camera acquisition, provider observation copy, scheduling, frame history, queue, replay or catch-up path. It uses fixed arrays/static descriptors/latest-only state; optional detail transform discovery is confined to binding/reference setup rather than per-frame hierarchy search. |
| Foundation E runtime wiring | **CORRECTION REQUIRED BEFORE USER QA** | `RichHumanoidDetailRetargeter.cs` exists, but its script GUID is absent from `PoseTrackingSpike.unity`; the Foundation E implementation diff added no bootstrap file that instantiates it, and `HumanoidRetargeter.ResolvePostSolveDetailLayers()` only discovers existing components. Prefer a code-owned spike/runtime composition fix that creates the E detail component without coupling provider-specific logic into the generic retargeter or relying on a manual scene edit. |
| Foundation E verification workflow | **READ-ONLY / CORE CODE-MATH GREEN / RUNTIME-WIRING GAP NOT COVERED** | `.github/workflows/foundation-e-retargeting.yml` retains A/B/C/D/E smokes, exact Phase-4 `ApplyMotionFrame(...)` comparison, generic detail boundary, body-only binding semantics, exactly eight production rich channels, provider/inference/history isolation, strict E-scope allowlist and clean/read-only tree guards. It also guards the parent-local absolute palm reference. Final run `34933354209`, job `104265988449`, succeeded at `087068dd14cd4bae11243a5efbd1bd5ee4cd309e`, but the workflow did not prove that a live spike actually instantiates the E component. |
| Foundation implementation order | **CURRENT EXECUTION PLAN — E RUNTIME WIRING FIX NEXT, THEN A–E USER QA** | A: command/speech → B: camera presets → C: rich orientation → D: hand landmarks are code-audited. E core/detail logic is implemented and verified, but runtime composition must be corrected and re-audited first. Then perform the comprehensive A–E USER Unity/manual/runtime QA pass before returning to Phase 5A. Do not start Phase 6 before that gate is advanced. |
| Foundation QA sequencing | **USER DECISION — MANUAL QA DEFERRED, NOT WAIVED** | Foundations A–E were allowed to continue sequentially through focused implementation/code/automated verification. USER manual/runtime acceptance remains deferred to one comprehensive pass; no foundation is auto-accepted and speech/camera/rich-orientation/hand/detail runtime QA remains pending until then. |
| Phase 6 | **NOT STARTED** | Do not begin Phase 6 until the USER explicitly advances the project beyond the current prerequisite/Phase 5A state. |